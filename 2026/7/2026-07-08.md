# Learning Journal - 2026-07-08

## 今日学习内容

### 1. Java（Day05）

学习主题：**数组（Array）**

学习内容：

- 数组的定义
- 数组的动态初始化
- 数组的静态初始化
- 数组元素访问
- 数组长度（length）
- 数组遍历
- 数组复制（Arrays.copyOf）
- 数组扩容

练习：

- 求数组中的最大值（MaxOfArray）

整理完成：

- Array.md
- MaxOfArray.md

---

### 2. AI-System-Learning

完善了 Java 知识库结构：

- 将数组相关知识统一整理到 `05-Array` 模块。
- 保持"一个章节一个 Markdown"的整理方式。
- 练习题单独放入 `Exercises` 目录。

---

## 今日收获

- 理解了数组用于存储相同类型数据的特点。
- 掌握了数组的两种初始化方式：动态初始化和静态初始化。
- 学会使用下标访问数组元素，并通过 `length` 获取数组长度。
- 熟悉了使用 `for` 循环遍历数组。
- 学会使用 `Arrays.copyOf()` 实现数组复制与扩容。
- 掌握了遍历数组求最大值的基本思想。

---

## 遇到的问题

- 需要注意数组下标从 **0** 开始，避免发生数组越界异常（ArrayIndexOutOfBoundsException）。
- 静态初始化只能在声明数组时使用，不能直接给已声明的数组赋值。

---

## 明日计划

- 继续学习 Java 下一章节内容。
- 整理新的知识点并同步更新到 GitHub。
- 按统一模板维护 AI-System-Learning 与 Learning-Journey。

---

## 今日总结

今天正式进入了 Java 数组的学习。数组是后续学习排序、查找、集合等内容的重要基础。通过数组的定义、初始化、遍历、复制 以及求最大值练习，进一步熟悉了循环与数组的结合使用。同时继续完善了知识库的整理规范，使后续学习内容更加系统、便于复习。